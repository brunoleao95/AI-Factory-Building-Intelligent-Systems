# =============================================================
# Backend COMPLETO (api.py) + app local full (app.py).
# Rodar na maquina do usuario (onde estao Ollama + deps pesadas).
#
#   pip install -r requirements-backend.txt
#   python -m spacy download pt_core_news_sm   (ou usar a URL pinada abaixo)
# =============================================================

# --- Web / API ---
streamlit==1.55.0
fastapi==0.135.1
uvicorn[standard]==0.41.0
sseclient-py==1.9.0
requests==2.33.1
python-dotenv==1.2.2
pydantic==2.12.5

# --- Dados / RAG ---
duckdb==1.5.1
chromadb==1.1.1
sentence-transformers==5.3.0
pypdf==6.9.2
pandas==2.3.3
numpy==2.4.3

# --- ML (AutoML) ---
scikit-learn==1.8.0
FLAML==2.6.0
ucimlrepo==0.0.7
joblib==1.5.3

# --- Agentes (CrewAI) ---
crewai==1.14.4
crewai-tools==1.14.4

# --- Observabilidade ---
langfuse==4.6.1

# --- Seguranca / Guardrails (Etapa 3) ---
# LLM Guard como lib e incompativel com Python 3.13 / torch 2.11 (pina
# torch==2.0.1, transformers==4.38.2). Usamos Presidio + os mesmos modelos HF
# diretamente -> equivalente. Ver src/guardrails.py.
transformers==5.4.0
torch==2.11.0
sentencepiece==0.2.1
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362
spacy==3.8.14
# Modelo spaCy pt (necessario para o Presidio). Wheel pinada:
https://github.com/explosion/spacy-models/releases/download/pt_core_news_sm-3.8.0/pt_core_news_sm-3.8.0-py3-none-any.whl
