# =============================================================
# Frontend LEVE - publicado no Streamlit Cloud (app_cloud.py).
# Apenas consome o backend via HTTP/SSE; nao carrega torch/chromadb/crewai.
# O backend pesado roda localmente -> ver requirements-backend.txt
# =============================================================
streamlit==1.55.0
requests==2.33.1
python-dotenv==1.2.2
